<?php

return [

    /*
    |--------------------------------------------------------------------------
    | CORS Paths
    |--------------------------------------------------------------------------
    |
    | Здесь указываются маршруты, к которым будет применяться CORS-middleware.
    |
    */
    'paths' => ['api/*', 'sanctum/csrf-cookie'],

    /*
    |--------------------------------------------------------------------------
    | Allowed HTTP Methods
    |--------------------------------------------------------------------------
    |
    | Разрешаем все методы (GET, POST, PUT, PATCH, DELETE и т.д.).
    |
    */
    'allowed_methods' => ['*'],

    /*
    |--------------------------------------------------------------------------
    | Allowed Origins
    |--------------------------------------------------------------------------
    |
    | Здесь перечислите все домены, с которых будет разрешён доступ.
    | Если хотите разрешить абсолютно всем — используйте ['*'].
    |
    */
    'allowed_origins' => [
        'http://localhost:3000',
        'https://breezy-onions-jump.loca.lt',
        // при необходимости добавьте другие адреса:
        // 'https://myapp.ngrok.io',
        // 'https://another-domain.com',
    ],

    /*
    |--------------------------------------------------------------------------
    | Allowed Origin Patterns
    |--------------------------------------------------------------------------
    |
    | Можно задавать шаблоны вместо полного URL, например:
    | ['*.loca.lt', 'https://*.mydomain.com']
    |
    */
    'allowed_origins_patterns' => [],

    /*
    |--------------------------------------------------------------------------
    | Allowed Headers
    |--------------------------------------------------------------------------
    |
    | Разрешаем все заголовки.
    |
    */
    'allowed_headers' => ['*'],

    /*
    |--------------------------------------------------------------------------
    | Exposed Headers
    |--------------------------------------------------------------------------
    |
    | Заголовки, которые клиент может читать.
    |
    */
    'exposed_headers' => [],

    /*
    |--------------------------------------------------------------------------
    | Max Age (seconds)
    |--------------------------------------------------------------------------
    |
    | Как долго браузер может кешировать результаты CORS-запроса.
    |
    */
    'max_age' => 0,

    /*
    |--------------------------------------------------------------------------
    | Supports Credentials
    |--------------------------------------------------------------------------
    |
    | Если вы используете куки или авторизацию по токенам с credentials.
    |
    */
    'supports_credentials' => true,

];
